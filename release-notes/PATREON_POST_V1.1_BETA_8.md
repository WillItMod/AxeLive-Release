# AxeLive V1.1_BETA_8

## Highlights
- Faster, lighter UI polling: `/api/status` now supports ETag/304 and short server-side caching.
- UI uses a lightweight status mode that avoids shipping full telemetry history for every device.

